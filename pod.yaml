apiVersion: v1
kind: pod
Metadata:
    name: my-pod
    lables:
        app: my-app
        env : dev
spec:
    containers :
    - name: my-container
      image: nginx:latest
      ports:
      - containerPort: 80

