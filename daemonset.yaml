apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: my-ds
spec:
  selector:
    matchLabels:
      app: ds-app   

  template:
    metadata:
      labels:
        app: ds-app   

    spec:
      containers:
      - name: nginx
        image: nginx